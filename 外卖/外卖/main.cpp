#include <iostream>
#include<stdio.h>
#include<stdlib.h>
#include"rider.h"
/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char** argv) {
	/*rid[0].achieve=1;
	rid[0].overtime=1;
	rid[0].receive=1;
	rid[0].exist=1;
	rid[1].exist=1;
	rid[1].achieve=1;
	rid[1].overtime=1;
	rid[1].receive=1;
	performance(rid);*///测试结构体可以使用 
	return 0;
}
